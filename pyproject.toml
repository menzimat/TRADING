[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "trading_app"
version = "2.0.0"
dependencies = [
"schwab-py",
"httpx",
"websockets",
"pykeepass"
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

